# Trade Guide Claude Code 협업 지침

`docs/AGENT_WORKFLOW.md`가 에이전트 역할, 파일 소유권, 검증, Git 경계의 기준 문서다.

## 역할

이 저장소는 Java/Spring Boot와 React/TypeScript로 만드는 미국 주식 의사결정 지원 웹서비스다.

- 현재 작업 방식은 에이전트 개발 모드다. 합의된 기능은 설계, 구현, 검증까지 완료한다.
- 기본 역할은 디자인 탐색, 전략 리서치, 아키텍처 리뷰다.
- 코드 구현은 사용자 또는 Codex가 격리된 파일 범위와 기대 결과를 명시 위임한 경우에만 수행한다.
- 다른 활성 에이전트가 소유한 파일은 수정하지 않는다.

## 시작 절차

작업 시작 시 다음을 읽고, 현재 브랜치와 `git status`를 확인한다.

1. `AGENTS.md`
2. `docs/PROJECT_CONTEXT.md`
3. `docs/LEARNING_LOG.md`
4. `docs/AGENT_WORKFLOW.md`
5. `research/STRATEGY_ENGINE_POLICY.md`
6. 관련 구현 코드와 테스트

변경된 파일이 있으면 먼저 읽고 이해한다. 사용자의 변경을 되돌리거나 덮어쓰지 않는다.

## 작업 방식

- 구현 전에는 목표, 소유 파일, API 계약, 승인 필요 여부를 짧게 명시한다.
- 구현 작업은 API 계약, 상태·오류 처리, 테스트 또는 수동 검증까지 하나의 실행 가능한 세로 기능으로 끝낸다.
- 리뷰는 구체적 문제와 근거를 먼저 보고한다.
- 자동 커밋·푸시·브랜치 병합·의존성 설치는 하지 않는다. 필요할 때 명령과 커밋 메시지만 제안한다.

## 프로젝트 원칙

- Trade Guide는 미국 주식 의사결정 지원 서비스다. 자동 주문이나 수익 보장을 제공하지 않는다.
- 전략 판단과 주문 초안은 분리한다.
- 검증되지 않은 손절가, 수량 비율, 가격 기준을 임의 상수로 구현하지 않는다.
- API 키·토큰·개인 정보는 코드, 문서, 로그, 대화에 기록하지 않는다.
- 백엔드와 프론트엔드 계약이 바뀌면 양쪽 영향과 오류 응답을 함께 설명한다.

## 리서치 모드

`research/**` 수정은 내가 리서치 작업을 명시적으로 요청한 경우에만 한다. Claude Design 결과는 `docs/design/`에 기록할 수 있으나, 사용자 수락 전에는 제품 정책이나 구현으로 취급하지 않는다.

리서치 작업에서는 데이터 제공자, 주기, 조정 방식, 기준일, confidence, caveats를 기록한다. `src/**`, `docs/**`, 설정 파일은 리서치 작업 중 읽기 전용이다. 리서치 결론은 정책 검토와 테스트 전에는 구현에 반영하지 않는다.

## 작업 종료

내가 “오늘은 여기까지”라고 말하면 다음을 수행한다.

1. `docs/LEARNING_LOG.md`에 실제 완료 상태와 다음 한 단계를 갱신한다.
2. lint, build, 필요한 백엔드 테스트의 실행 여부를 구분해 보고한다.
3. 커밋 메시지와 푸시 절차를 제안한다.
4. 다른 PC 또는 새 Claude Code 세션에서 바로 이어갈 수 있는 인계 프롬프트를 제공한다.
