# Local development only. Do not set this in a deployed environment.
# When omitted, the application reads the current Google login from /api/auth/me.
VITE_LOCAL_MEMBER_ID=1
# Backend target for the Vite development proxy. Defaults to http://localhost:8080.
VITE_API_PROXY_TARGET=http://localhost:8081
